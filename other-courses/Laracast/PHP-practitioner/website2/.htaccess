<IfModule mod_rewrite.c>
    RewriteEngine On

    RewriteBase /wendyvancraen/learning-php/other-courses/Laracast/PHP-practitioner/website2/

    RewriteCond %{REQUEST_FILENAME} !-f 
    RewriteCond %{REQUEST_FILENAME} !-d

    RewriteRule ^(.*)$ index.php
</IfModule>